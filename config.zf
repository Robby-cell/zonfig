{
  # name of the project
  name = "zonfig",
  # version
  version = "0.1.0",
  # year of creation
  year = 2024,

  # files associated with the project
  files = [
    "src/field.zig",
    "src/Lexer.zig",
    "src/main.zig",
    "src/root.zig",
    "src/Zonfig.zig",
    "src/Parser.zig",
    "src/Token.zig"
  ],

  # commands available
  cmd = {
    run = "run",
    test = "test",
  }
}
